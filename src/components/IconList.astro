---
const { stacks } = Astro.props;
import { Icon } from 'astro-icon/components';
---

{
  stacks.map((stack) => (
    <>
      <Icon
        name={stack}
        className="z-40 transition-all duration-300 group-hover:translate-x-1"
        size={28}
      />
    </>
  ))
}
