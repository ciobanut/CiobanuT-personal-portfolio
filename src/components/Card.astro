---
const { classNames } = Astro.props;
---

<article class=`border border-slate-200 rounded-2xl p-2 dark:border-slate-700 ${classNames}`>
	<div
		class=`flex flex-col gap-2 p-10 bg-slate-50 rounded-xl justify-center h-full `
	>
		<slot />
	</div>
</article>
