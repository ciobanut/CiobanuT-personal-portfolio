---
const { classNames } = Astro.props;
---

<article class=`border border-slate-200 rounded-2xl p-2 dark:border-stone-700/60 ${classNames}`>
	<div
		class=`flex flex-col gap-2 p-10 bg-slate-50 rounded-xl justify-center h-full dark:bg-stone-800 `
	>
		<slot />
	</div>
</article>
