---
import { Icon } from 'astro-icon/components';
const { question, answer, id } = Astro.props;
---

<div
	class="relative flex flex-wrap justify-between overflow-hidden border border-slate-200/60 rounded-lg dark:border-slate-700/60"
>
	<label
		class="grow font-normal cursor-pointer p-3 z-10 text-slate-800 dark:text-slate-200 pr-8"
		for={id}
	>
		{question}</label
	>
	<input
		class="peer h-0 w-0 appearance-none"
		type="checkbox"
		id={id}
	/>
	<Icon
		name="ph:plus"
		class="absolute duration-200 peer-checked:rotate-45 right-3 top-0 transition-all translate-y-2/3 z-0"
		size={20}
	/>

	<div
		class="absolute duration-0 opacity-0 p-3 peer-checked:opacity-100 peer-checked:relative peer-checked:scale-y-100 pt-0 scale-y-0 text-slate-500 text-sm dark:text-slate-400"
	>
		{answer}
	</div>
</div>
