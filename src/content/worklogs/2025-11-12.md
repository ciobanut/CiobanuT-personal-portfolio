---
title: 'Static Files to Theme, Sitemap and Canonical'
pubDate: '12-11-2025'
description: 'Copied all static files into the landing soft.cy theme, added sitemap and canonical URLs, and reverted lead changes.'
stack:
  - 'laravel'
  - 'php'
  - 'blade'
  - 'seo'
github_repos:
  - 'zordecmax/landing'
---

# 12 Nov 2025

Copied all static files into the landing `soft.cy` theme, added sitemap and canonical URLs, and reverted lead changes.

## Projects

### landing

**Static files.** Copied all static files to the `soft.cy` theme (+17,022/−75) — the theme becomes self-contained.

**SEO.** Added sitemap and canonical URLs (+4/−4).

**Leads.** Removed changes in `LeadController` and the lead route, then reverted back to the `HomeController` and `LeadController` split (+25/−41).

### All changes

- Copied static files into soft.cy theme
- Added sitemap and canonical URLs
- Reverted/re-applied lead controller changes

## Lessons

Making a theme self-contained (+17,022) means it can be deployed standalone — a big structural move that consolidates assets.

## Next

- Tailwind install
- Continue landing
