---
title: 'MUI Theme Provider, Env Setup'
pubDate: '04-02-2026'
description: 'Integrated the MUI theme provider into taxi-passenger and set up .env handling.'
stack:
  - 'react'
  - 'nextjs'
  - 'typescript'
  - 'mui'
github_repos:
  - 'zordecmax/taxi-passenger'
---

# 4 Feb 2026

Integrated the MUI theme provider into taxi-passenger and set up `.env` handling.

## Projects

### taxi-passenger

**MUI theme.** Integrated the MUI theme provider and removed an unnecessary button component (+219/−78).

**Env setup.** Updated `.gitignore` to include `.env` and added a `.env.example` (+190) for reproducible local setup.

### All changes

- Integrated MUI theme provider
- Added .env.example; gitignored .env

## Lessons

A theme provider makes the app's look a single config instead of scattered styles — the first commit of any MUI adoption.

## Next

- SPA auth flow
- Continue passenger UI
