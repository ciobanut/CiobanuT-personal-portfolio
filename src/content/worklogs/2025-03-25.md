---
title: 'MinIO Port Configuration'
pubDate: '25-03-2025'
description: 'Configured the MinIO API port and routing in MyPrivateOrder infrastructure.'
stack:
  - 'docker'
  - 'minio'
  - 'traefik'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 25 Mar 2025

Configured the MinIO API port and routing in MyPrivateOrder infrastructure.

## Projects

### myprivateorder.com

**MinIO.** Iterated on MinIO ports — API on 9999 (+6), back to the default, then S3 API port 9999 and port 9000 in Traefik (+2), finally fixing port 9000 (+25/−39).

### All changes

- Configured MinIO API port and Traefik routing

## Lessons

MinIO's two ports (API + console) cause endless routing confusion — the day's many commits are the process of pinning down which port serves S3.

## Next

- MinIO volume/subdomain
- Continue infra
