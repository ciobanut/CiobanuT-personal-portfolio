---
title: 'Build on VPS, Separate Docker Stacks'
pubDate: '20-02-2025'
description: 'Moved the MyPrivateOrder build to the VPS, added local and prod docker-stack files, and seeded countries/ports.'
stack:
  - 'docker'
  - 'docker-compose'
  - 'github-actions'
  - 'laravel'
  - 'php'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 20 Feb 2025

Moved the MyPrivateOrder build to the VPS, added local and prod docker-stack files, and seeded countries/ports.

## Projects

### myprivateorder.com

**Build location.** Moved the build to run on the VPS instead of GitHub (+8/−17), adding a composer update on build (+1/+2) and `libssl-dev` (+2).

**Docker stacks.** Added two different docker-stack files — local and prod (+201/−140).

**Seeding.** Seeded countries and ports first in the seed order (+1).

### All changes

- Built on the VPS instead of GitHub
- Added separate local/prod docker-stack files
- Ordered country/port seeding first

## Lessons

Two docker-stack files (local vs prod) keep environment differences explicit — the local/prod split is the standard way to run different configs from one repo.

## Next

- First deploy to fresh VPS
- Continue infra
