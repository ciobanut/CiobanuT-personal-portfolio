---
title: 'Standalone Banner Preview Page'
pubDate: '01-07-2026'
description: 'Added a standalone banner preview page to behavora built with Tailwind and MaryUI, plus a container health-check route for debugging.'
stack:
  - 'laravel'
  - 'php'
  - 'livewire'
  - 'tailwindcss'
  - 'maryui'
github_repos:
  - 'zordecmax/journey-predictor-api'
---

# 1 Jul 2026

Added a standalone banner preview page to behavora and rebuilt the preview UI on Tailwind + MaryUI, plus a container health-check route.

## Projects

### behavora (journey-predictor-api)

**Preview banner page.** Added a full preview banner page (+853) so banners can be reviewed outside the editor context.

**UI rewrite.** Replaced the custom CSS on the preview page with Tailwind + MaryUI (−419 net), removing hand-rolled styles in favor of the design system.

**Debug tooling.** Added a route to test whether the app is responding from the container, plus more debug buttons in the banner editor actions; removed unnecessary debug buttons and a route test.

### All changes

- Added standalone preview banner page (+853)
- Rebuilt preview page on Tailwind + MaryUI
- Added container health-check route and editor debug buttons
- Removed redundant debug buttons

## Lessons

A standalone preview page decouples "how a banner looks" from the editor surface — worth the +853 once banner config got rich enough to review outside edit mode.

## Next

- Demo mode and error overlay in preview
- Continue display timing rules
