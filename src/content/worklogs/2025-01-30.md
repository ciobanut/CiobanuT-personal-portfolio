---
title: 'Nginx and Dockerfile Copy Paths'
pubDate: '30-01-2025'
description: 'Fixed nginx.conf and the Dockerfile copy paths in MyPrivateOrder.'
stack:
  - 'nginx'
  - 'docker'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 30 Jan 2025

Fixed nginx.conf and the Dockerfile copy paths in MyPrivateOrder.

## Projects

### myprivateorder.com

**Nginx.** Fixed `nginx.conf` (+9/+3) and the nginx setup in the Dockerfile (+1/−1).

**Dockerfile.** Fixed the Dockerfile copy paths (+36/−43, +2/−3) so the right files reach the image.

### All changes

- Fixed nginx.conf
- Fixed Dockerfile copy paths

## Lessons

Copy-path errors in a Dockerfile are silent until the image runs — the nginx config and copy paths are the usual culprits.

## Next

- Dockerfile + nginx.conf updates
- Continue infra
