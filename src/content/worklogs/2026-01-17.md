---
title: 'Invitatii Docker Deploy Workflows'
pubDate: '17-01-2026'
description: 'Added Docker build and deployment workflows to a VPS for invitatii, improved the Dockerfile, and streamlined the deployment script.'
stack:
  - 'php'
  - 'docker'
  - 'github-actions'
github_repos:
  - 'ciobanut/invitatii_nunta'
---

# 17 Jan 2026

Added Docker build and deployment workflows to a VPS for invitatii, improved the Dockerfile, and streamlined deployment.

## Projects

### invitatii

**CI/CD.** Added build and deployment workflows for a Docker image to a VPS (+58).

**Dockerfile.** Updated the Dockerfile and configuration for improved builds (+174/−26), optimizing composer install and adjusting memory.

**Deployment script.** Streamlined the deployment script by moving repository cloning earlier (+10/−9), added `docker ps` and sleeps before starting services, removed old git-cloned directories, specified the Docker Compose project name, and fixed the `env_file` path.

### All changes

- Added Docker build/deploy workflows to VPS
- Improved Dockerfile (composer optimization)
- Streamlined deployment script

## Lessons

Docker deployments are scripting-heavy — the iteration on sleeps, project names and env_file paths shows how many small assumptions a working deploy actually encodes.

## Next

- docker-compose for app services
- Continue invitatii infra
