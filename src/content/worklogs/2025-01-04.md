---
title: 'Cleanup: City Controller, Fly.io Files, Ports SQL'
pubDate: '04-01-2025'
description: 'Removed the unused city controller and Fly.io files from MyPrivateOrder, and split the ports SQL into chunks.'
stack:
  - 'laravel'
  - 'php'
  - 'sql'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 4 Jan 2025

Removed the unused city controller and Fly.io files from MyPrivateOrder, and split the ports SQL into chunks.

## Projects

### myprivateorder.com

**Cleanup.** Removed the city controller and views (+0/−1,404) and the Fly.io files (+0/−1,089) — dead code from abandoned hosting. Moved images from storage to the public folder (+27/−114) and removed the storage gitignore.

**Data.** Split `ports.sql` into 1000-row chunks (+14) and cleaned up `CREATE TABLE` statements and backticks in `ports.sql`/`countries.sql` (+28/+10).

### All changes

- Removed city controller + Fly.io files
- Moved images to public
- Split ports.sql into chunks

## Lessons

Removing abandoned hosting files and unused controllers is a −2,500 cleanup — dead code from pivots gets deleted, not archived.

## Next

- Dockerfile + UserSeeder
- Continue MyPrivateOrder
