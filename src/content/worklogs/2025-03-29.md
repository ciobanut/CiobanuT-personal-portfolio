---
title: 'MinIO Volume and S3 Subdomain'
pubDate: '29-03-2025'
description: 'Added a MinIO volume and S3 subdomain on the MyPrivateOrder infrastructure.'
stack:
  - 'docker'
  - 'minio'
  - 's3'
  - 'traefik'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 29 Mar 2025

Added a MinIO volume and S3 subdomain on the MyPrivateOrder infrastructure.

## Projects

### myprivateorder.com

**MinIO.** Added the `/mnt/minio` volume (+1/−3), added a subdomain for S3 (+4/−4), fixed the MinIO address in the stack file (+1), and removed the 9000-port entrypoint (+4).

### All changes

- Added MinIO volume + S3 subdomain
- Fixed MinIO stack address

## Lessons

Object storage via MinIO needs a persistent volume and a clean routing path — the subdomain fix is what makes S3 URLs stable.

## Next

- n8n and minio keys
- Continue infra
