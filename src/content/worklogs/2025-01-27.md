---
title: 'Dockerfile, Nginx, Nixpacks'
pubDate: '27-01-2025'
description: 'Updated the MyPrivateOrder Dockerfile and nginx.conf, and tried nixpacks for builds.'
stack:
  - 'docker'
  - 'nginx'
  - 'nixpacks'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 27 Jan 2025

Updated the MyPrivateOrder Dockerfile and nginx.conf, and tried nixpacks for builds.

## Projects

### myprivateorder.com

**Docker/nginx.** Updated the Dockerfile and nginx.conf (+10/−3), exposed port 3000 (+2), and added an nginx config for cache-control headers (+11/−55).

**Nixpacks.** Added `nixpacks.toml` (+180) then removed it (+0/−179) after evaluating the build tool, and tracked the Docker untracked files (+95).

### All changes

- Updated Dockerfile + nginx.conf
- Tried then dropped nixpacks
- Added nginx cache-control config

## Lessons

Evaluating nixpacks and dropping it is a build-tool decision — the add/remove pair records the evaluation.

## Next

- .htaccess cache-control
- Continue infra
