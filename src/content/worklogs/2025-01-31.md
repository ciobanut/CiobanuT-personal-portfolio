---
title: 'Nginx Cache Control, Header Buffers'
pubDate: '31-01-2025'
description: 'Added cache-control configuration and large client header buffers to the MyPrivateOrder nginx config.'
stack:
  - 'nginx'
  - 'docker'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 31 Jan 2025

Added cache-control configuration and large client header buffers to the MyPrivateOrder nginx config.

## Projects

### myprivateorder.com

**Nginx.** Added `large_client_header_buffers` (+5) and a cache-control config file (+26/+9), then moved the cache-control into the main nginx.conf (+1/−51).

### All changes

- Added cache-control config
- Added large client header buffers

## Lessons

Nginx caching headers and buffer sizes are the two knobs that fix most "slow site" and "400 header too large" issues.

## Next

- Nginx fixes in Dockerfile
- Continue infra
