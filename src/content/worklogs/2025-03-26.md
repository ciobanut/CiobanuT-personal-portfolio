---
title: 'MinIO Config, FilePond, Package Upgrade'
pubDate: '26-03-2025'
description: 'Added MinIO config with access keys, the sopamo/laravel-filepond package, and upgraded all dependencies on MyPrivateOrder.'
stack:
  - 'laravel'
  - 'php'
  - 'docker'
  - 'minio'
  - 'filepond'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 26 Mar 2025

Added MinIO config with access keys, the `sopamo/laravel-filepond` package, and upgraded all dependencies on MyPrivateOrder.

## Projects

### myprivateorder.com

**MinIO.** Added MinIO config (+17/−6), the access key in the docker stack (+7), and a MinIO user. Started saving the avatar to S3 (+1).

**FilePond.** Added `sopamo/laravel-filepond` (+50).

**Dependencies.** Upgraded all package dependencies (+927/−614).

### All changes

- Added MinIO config + access keys
- Added laravel-filepond
- Upgraded all dependencies

## Lessons

MinIO + FilePond is the object-storage stack: the dependency upgrade commit is the enabler that makes the newer packages work.

## Next

- MinIO port/route fixes
- Continue infra
