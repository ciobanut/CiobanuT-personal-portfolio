---
title: 'Docker Compose Fixes'
pubDate: '30-10-2025'
description: 'Fixed MyPrivateOrder docker-compose by commenting out the shared Nginx volume and gitignoring the env file.'
stack:
  - 'docker'
  - 'docker-compose'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 30 Oct 2025

Fixed MyPrivateOrder docker-compose by commenting out the shared Nginx volume and gitignoring the env file.

## Projects

### myprivateorder.com

**Docker.** Commented out the shared volume for Nginx in docker-compose (+4/−2), added `.env.myprivateorder` to `.gitignore` (+1), and fixed an error.

### All changes

- Commented out shared Nginx volume
- Gitignored env file

## Lessons

Shared volumes across containers cause subtle stale-file bugs — commenting it out isolates Nginx's view of the app.

## Next

- Continue docker/infra work
