---
title: 'Tracking Services for Sessions and Journeys'
pubDate: '06-04-2026'
description: 'Implemented behavora tracking services for user sessions and journeys with validation, and added funnel_stage with SyncSessionFeatures.'
stack:
  - 'laravel'
  - 'php'
  - 'livewire'
github_repos:
  - 'zordecmax/journey-predictor-api'
---

# 6 Apr 2026

Implemented behavora tracking services for user sessions and journeys with validation, and added `funnel_stage` computation.

## Projects

### behavora (journey-predictor-api)

**Tracking services.** Implemented tracking services for user sessions and journeys, adding validation (+436/−218) — the ingestion path now has explicit service boundaries and input validation.

**Funnel stage.** Added a `funnel_stage` field to `UserSession` and implemented `SyncSessionFeatures` (+145/−175) to compute and sync the funnel stage per session.

### All changes

- Implemented session/journey tracking services with validation
- Added funnel_stage field + SyncSessionFeatures

## Lessons

Extracting tracking services with validation made ingestion deterministic — the controller delegates, and every payload shape is enforced at the service boundary.

## Next

- Personal banner sending
- Continue session tracking
