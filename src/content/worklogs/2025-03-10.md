---
title: 'Swarm Volumes, Migrations in Deploy'
pubDate: '10-03-2025'
description: 'Fixed MyPrivateOrder Swarm volume names, reorganized the docker files, and ran migrations in the deploy pipeline.'
stack:
  - 'docker'
  - 'swarm'
  - 'github-actions'
  - 'nginx'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
---

# 10 Mar 2025

Fixed MyPrivateOrder Swarm volume names, reorganized the docker files, and ran migrations in the deploy pipeline.

## Projects

### myprivateorder.com

**Volumes.** New volume names in Swarm (+16/−16), an `app_storage` volume for `/var/www/html/storage` (+2), and the `APP_VOLUME_PLACEHOLDER` via sed in GitHub Actions (+6/−30).

**Deploy.** Ran `php migrate` in the container after deploy (+21), then reverted and re-applied the approach (+20/−15). Fixed the path to the env file in the prod stack (+6).

**Nginx.** Went back to the nginx service on the official image (+22/−23), adding an nginx Dockerfile (+7).

### All changes

- Fixed Swarm volume names + app_storage
- Ran migrations post-deploy
- Back to official nginx image

## Lessons

Swarm volume names and env paths are the classic prod-only breakers — the deploy pipeline commits encode the lessons learned in staging.

## Next

- Varnish removal, auth flag
- Continue infra
