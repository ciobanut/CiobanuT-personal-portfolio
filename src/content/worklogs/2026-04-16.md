---
title: 'UTM Tracking, Card Stats, Threshold Modal'
pubDate: '16-04-2026'
description: 'Implemented UTM tracking for behavora visitor sessions, added a card-stats component and an edit-banner-threshold modal.'
stack:
  - 'laravel'
  - 'php'
  - 'livewire'
github_repos:
  - 'zordecmax/journey-predictor-api'
---

# 16 Apr 2026

Implemented UTM tracking for behavora visitor sessions and added dashboard components (card stats, banner threshold modal).

## Projects

### behavora (journey-predictor-api)

**UTM tracking.** Implemented UTM tracking for visitor sessions and enhanced session data (+402/−68) — campaign parameters are captured per session.

**Dashboard components.** Added a `card-stats` component and an `edit-banner-threshold` modal for the website view (+190/−34). Fixed the banner show-threshold display to use the correct settings variable.

**Fix.** Updated the overflow property on the `stat-item` component for proper scrolling (+31/−34).

### All changes

- Implemented UTM tracking for sessions (+402)
- Added card-stats component and threshold modal
- Fixed show-threshold variable and stat-item overflow

## Lessons

UTM params are cheap to capture at session creation and expensive to reconstruct later — storing them at ingest makes campaign attribution a first-class query.

## Next

- Link predictions/impressions to journeys
- Continue dashboard components
