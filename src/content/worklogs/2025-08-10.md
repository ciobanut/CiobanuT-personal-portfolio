---
title: 'Docker Swarm to Compose Migration'
pubDate: '10-08-2025'
description: 'Migrated MyPrivateOrder infrastructure from Docker Swarm to docker-compose, moving Traefik into a separate compose file and optimizing GitHub Actions.'
stack:
  - 'docker'
  - 'docker-compose'
  - 'traefik'
  - 'github-actions'
github_repos:
  - 'ciobanut/MyPrivateOrder.com'
  - 'ciobanut/CiobanuT-personal-portfolio'
---

# 10 Aug 2025

Migrated MyPrivateOrder infrastructure from Docker Swarm to docker-compose, extracting Traefik and optimizing the deploy workflow.

## Projects

### myprivateorder.com

**Swarm → Compose.** Moved to docker-compose (+102/−33), extracted Traefik into a separate compose file (+32/−397), and optimized the GitHub Actions rebuild workflow (+72/−19). Fixed paths to env and Dockerfile, moved repository cloning into the workflow file (+8/−8), added the `travl.fun` compose file (+128), and checked n8n.

### astro-portfolio-website

Updated the "How I Got Typesense Running in Docker Swarm Using a Secret" post (+20/−2) to reflect the migration.

### All changes

- Moved from Docker Swarm to docker-compose
- Extracted Traefik to its own compose file
- Optimized GitHub Actions deploy
- Updated Typesense swarm post

## Lessons

Docker Swarm → Compose is an infra migration with a long tail — the day's many small fixes (paths, env, contexts) are the migration's real cost.

## Next

- Continue compose migration
- Traefik compose split
