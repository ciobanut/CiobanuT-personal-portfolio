---
import { SEO } from 'astro-seo'
import { ClientRouter } from 'astro:transitions'
import Header from '../Base/Header.astro'
import Footer from '../Base/Footer.astro'
import Navigation from './Navigation/Navigation.astro'
import Metrika from '@components/Metrika.astro'

import '../../styles/global.css'
//import '@fontsource/inter';

import config from '../../../config.js'

let { pageTitle = '', pageDescription = '', canonical, prevHref, nextHref, ogUrl } = Astro.props

pageTitle += ' - ' + config.websiteName
---

<!doctype html>
<html transition:animate="none" lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="generator" content={Astro.generator} />

		<!-- Fonts -->
		<link href="https://fonts.googleapis.com" rel="preconnect" />
		<link href="https://fonts.gstatic.com" rel="preconnect" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Inter:ital,wght@0,100..900;1,100..900&display=swap"
			rel="preload"
			crossorigin
			as="style"
		/>

		<SEO
			title={pageTitle}
			description={pageDescription}
			canonical={canonical}
			openGraph={{
				basic: {
					title: pageTitle,
					type: 'website',
					image: '/favicon.svg',
					url: ogUrl
				}
			}}
			extend={{
				// extending the default meta tags
				meta: []
			}}
		/>

		{prevHref && <link rel="prev" href={prevHref} />}
		{nextHref && <link rel="next" href={nextHref} />}

		<ClientRouter />

		<Metrika />

	</head>

	<body class="m-5 bg-slate-50 dark:bg-stone-950 sm:mx-8">
		<main
			class="mx-auto mb-28 flex max-w-4xl flex-col gap-30 rounded-3xl border border-slate-200 bg-white p-5 dark:border-stone-800 dark:bg-stone-900 sm:px-10 sm:py-8"
		>
			<Header transition:persist="header" />

			<slot />

			<Footer transition:persist="footer" />
		</main>

		<Navigation transition:persist="navigation" />

</body>
</html>
